# QRD: {recipe_title} ({view_name})

> QA Requirements Document — auto-generated by the QA agent, reviewed and approved by a human.

## Transformation Summary

{Plain-language description of what the recipe does: what data it combines, how it transforms it, and what the output represents.}

## Input Contract

| Table | Column | Type | Notes |
|-------|--------|------|-------|
| {source_table} | {column_name} | {String/Float64/Date/...} | {role: key, metric, dimension, filter, etc.} |

## Output Contract

| Column | Type | Semantic | Notes |
|--------|------|----------|-------|
| {column_name} | {String/Float64/Date/...} | {what this column represents} | {derivation: direct pass-through, calculated, aggregated, etc.} |

## Data Quality Rules

| # | Rule | SQL Expression | Failure Condition |
|---|------|---------------|-------------------|
| 1 | {human-readable rule description} | {SQL predicate that must be TRUE for valid rows} | {when this rule is considered violated} |

Examples of common rules:
- `spend >= 0` — monetary values must be non-negative
- `campaign_id IS NOT NULL` — key columns must not be NULL
- `impressions >= clicks` — impressions must be >= clicks

## Deduplication Rules

- **Grain columns:** {comma-separated list of columns that define uniqueness, e.g. date, campaign_id, datasource}
- **Dedup strategy:** {e.g. "keep latest by updated_at", "no dedup — expect exactly 1 row per grain combination", "sum metrics within grain"}

## Business Logic Rules

| # | Rule | SQL Expression | Failure Condition |
|---|------|---------------|-------------------|
| 1 | {human-readable description of the business invariant} | {SQL expression that must hold, e.g. abs(cpc - spend/nullIf(clicks, 0)) < 0.01} | {what it means when this fails} |

Examples:
- CPC = spend / clicks (with NULL for zero clicks)
- CTR = clicks / impressions (bounded 0..1)
- Total spend = sum of channel spends

## Row Count Expectations

- **Expected range:** {e.g. "output rows between 80%-120% of max input table row count", or "output rows > 0"}
- **SQL check:**
```sql
-- {Parameterized query to verify row count bounds}
SELECT count(*) AS row_count
FROM <db>.{view_name}
WHERE date >= toStartOfMonth(today())
```

## Freshness Rules

- **Max staleness:** {e.g. "48 hours" — max acceptable gap between max(date) in output and current time}
- **SQL check:**
```sql
SELECT
    max(date) AS latest_date,
    dateDiff('hour', max(date), now()) AS hours_stale
FROM <db>.{view_name}
```

## Version History

| Version | Date | Changes | Session |
|---------|------|---------|---------|
| v1.0 | {YYYY-MM-DD} | Initial generation from recipe analysis | {session_id} |
